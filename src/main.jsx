
import React from "react";
import ReactDOM from "react-dom/client";
import ContractTemplateLayout from "./ContractTemplateLayout";

ReactDOM.createRoot(document.getElementById("root")).render(
  <React.StrictMode>
    <ContractTemplateLayout />
  </React.StrictMode>
);
